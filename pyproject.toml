[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "just-buildit"
version = "0.1.0"
description = "Minimum viable PEP 517 build backend for C extensions."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.urls]
Homepage = "https://github.com/just-buildit/just-build"
Repository = "https://github.com/just-buildit/just-build"
"Bug Tracker" = "https://github.com/just-buildit/just-build/issues"
