[build-system]
requires = ["just-buildit"]
build-backend = "just_build"

[project]
name = "calc"
version = "0.1.0"
description = "just-build mixed pure/platform example."
requires-python = ">=3.11"

[tool.just-build]
command = "make"
repair = false
