[build-system]
requires = ["just-buildit"]
build-backend = "just_build"

[project]
name = "add"
version = "0.1.0"
description = "just-build CMake example."
requires-python = ">=3.11"

[tool.just-build]
command = "make pyext"
repair = false
